# Week11 AIと考えたログ

## Part 1：AIと考えた1セッション

### テーマ
week09のJSの部分の書き方の意味を聞く

### 送った指示（良いプロンプトの型）

目的:week09のJavaScriptの部分についてなぜこのように書いたのかを説明してもらう
前提:JavaScriptの部分に関しては６割くらいわかっており、コードの流れはそこまで理解することができていない
見てほしい観点:コード（JavaScript）の部分の流れ
制約:流れについてのみの説明
出力形式:文章

### 返ってきた出力（要約）

フォームとメッセージ表示場所を取得する

どのフォームを監視するか

どこにエラーや成功メッセージを出すか
→ ここで「対象」を決める。

フォーム送信イベントを設定する

submit が起きた瞬間に、指定した処理が全部実行される。

ページのリロードを止める

event.preventDefault()  
→ HTML の通常の送信動作を止めて、JS で処理するモードに切り替える。

入力された値を取得する

.value で文字を取り出す

.trim() で余計なスペースを除去

.checked でチェックボックスの状態を取得
→ バリデーションに使える形に整える。

バリデーションを上から順番にチェックする

名前が空ならエラー

メールが空ならエラー

パスワードが短ければエラー

規約に同意していなければエラー
→ エラーが出たらメッセージ表示＋return で処理を止める。

すべて OK なら成功メッセージを表示する

緑色のメッセージに切り替え

「ようこそ ○○ さん！」と歓迎する

### "なぜ"を聞いた記録

- 聞いたこと:なぜ.valueで文字を取り出すことができるのか？
- 返ってきた説明（要約）:<input>というオブジェクトにはvalue,checked,type,nameのようなプロパティがもともと備わっていて、つまり、<input> はただのタグではなく、ブラウザが「入力欄としての機能」を持たせた特別なオブジェクトということ
- 聞く前は分からなかったが、聞いて分かったこと:<input>というタグは特別なオブジェクトであるということ

### 出力の4分類

| 分類 | 内容 | 判断理由 |
|---|---|---|
| 採用する |event.preventDefault()  
→ HTML の通常の送信動作を止めて、JS で処理するモードに切り替える。 |英語の単語の意味的にも正しいと感じたから。 |
| 保留する |JavaScript で document.getElementById("name") をすると、
ブラウザは HTML の <input> を JavaScript のオブジェクトとして返す。 |自分がまだ意味を完全には理解できていないため。 |
| 採用しない | | |
| 要確認 |エラーが出たらメッセージ表示＋return で処理を止める |returnの意味について自分は理解できていないが、言葉の意味的に少しニュアンスが違うのではないかと思ったから。 |

---

## Part 2：流された場面の振り返り

week09でJavaScriptの部分で意味は完全には理解することはできなかったが要件は満たしていたため。

---

## Part 3：今後のAI利用方針

### Week11で気づいたこと

特にJavaScriptの部分で自分が理解せずに使っているところがまだまだ多いなと感じた。

### 自分のAI利用ルール

tailwindcssとJavaScriptはそれぞれのコードの意味と流れを理解するまで同じweekをやり続ける。